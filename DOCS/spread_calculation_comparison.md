# Strategy reference

The current sampling, spread, and exposure formulas are documented in [SPREAD_CALCULATION.md](../SPREAD_CALCULATION.md).

`python_bt_OBI.py` is a historical backtest reference. Its sampling and execution assumptions are not an acceptance benchmark for the live Rust strategy. Compare governing definitions and representative observations rather than requiring matching outputs.
