services:
  collector:
    build: .
    container_name: lighter-ohlcv-collector
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - SYMBOLS=BTC,ETH,HYPE
      - EARLIEST_DATE=2025-01-15
      - POLL_INTERVAL=14400
